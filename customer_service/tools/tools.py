def generate_document(document_content: str, file_name: str = "meeting_summary.docx") -> str:
    """
    A dummy tool that pretends to generate a document.
    """
    return f"Successfully created the document: {file_name}"